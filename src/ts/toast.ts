/**
 * A class representing a toast notification.
 * A call to `Toast.Create("...")` displays a new toast on the screen.
 */
class Toast
{
// Static fields ==================================================================================
    
    static _activeToasts: Toast[] = [];

    static Create(msg: string)
    {

    }

// Instance fields ================================================================================

    private constructor()
    {

    }
}